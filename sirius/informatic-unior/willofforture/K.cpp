#include <bits/stdc++.h>
typedef long long ll;

using namespace std;

int main() {
    	ios_base::sync_with_stdio(false);
    	cin.tie(0);
    	cout.tie(0);
	ll a, b, ans = 0;
	cin >> a >> b;
	for (ll i = a; i <= b; i++) {
		ll now = 0;
		for (auto i : to_string(i)) {
			 
}
