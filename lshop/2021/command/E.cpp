#include <bits/stdc++.h>
typedef long long ll;

using namespace std;

int main() {
    	ios_base::sync_with_stdio(false);
    	cin.tie(0);
    	cout.tie(0);
	ll h, w, n;
	cin >> h >> w >> n;
	ll sl, sr;
	cin >> sl >> sr;
	string s1, s2;
	cin >> s1 >> s2;

}
