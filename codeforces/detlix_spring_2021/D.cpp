#include <bits/stdc++.h>
typedef long long ll;

using namespace std;

int main() {
    	ios_base::sync_with_stdio(false);
    	cin.tie(0);
    	cout.tie(0);
	cout << 1000 << endl;
	for (ll i = 0; i < 10; i++) {
		cout << 1000 << ' ' << 1000 << endl;
		for (ll i = 0; i < 1000; i++) {
			cout << 0;
		}
		cout << endl;
	}
}
