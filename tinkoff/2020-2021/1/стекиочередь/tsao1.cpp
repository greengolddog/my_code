#include <bits/stdc++.h>
using namespace std;

vector<vector<char>> split(string s) {
    vector<vector<char>> ans;
    vector<char> p;
    ans.push_back(p);
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int d;
    string a;
    cin >> a;
    int pp = 0;
    vector<int> s;
    for (ll 
}
